{
  "name": "fresh-meat-nepal",
  "version": "1.0.0",
  "private": true,
  "description": "Fresh Meat Nepal - MERN e-commerce platform for fresh and processed meat products in Nepal",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "setup": "node scripts/setup.js",
    "doctor": "node scripts/doctor.js",
    "mail:test": "npm run mail:test --prefix server --",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "npm-run-all --parallel dev:server dev:client dev:admin",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix admin",
    "start": "npm start --prefix server",
    "ssl:dev": "node scripts/ssl-dev.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "selfsigned": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
