# The admin panel must never appear in a search result. The noindex meta tag in
# index.html says the same thing, but only to a crawler that fetches and parses
# the page first; this refuses it before that.
User-agent: *
Disallow: /
